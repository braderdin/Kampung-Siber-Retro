// Start: Site Files Page - Delegates to Unified FileManager Component
// Non-destructive consolidation: route preserved, rendering delegated to shared FileManager.
import FileManager from '@/components/FileManager';

export default function SiteFilesPage() {
  return <FileManager />;
}
// End: Site Files Page